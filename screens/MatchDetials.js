import { StyleSheet, Text, View } from "react-native";
import React from "react";

const MatchDetials = ({ navigation }) => {
   return (
      <View>
         <Text>MatchDetials</Text>
      </View>
   );
};

export default MatchDetials;

const styles = StyleSheet.create({});
